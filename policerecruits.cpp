#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int n;
    cin >> n;

    vector<int> arr;

    while (n--)
    {
        int tmp;
        cin >> tmp;
        arr.push_back(tmp);
    }

    int left = 0;

    for (int i = 1; i < n; ++i)
    {
        
    }
    
    return 0;
}