#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    string s;
    cin >> s;

    string res;
    int countLower = 0, countUpper = 0;
    for (int i = 0; i < s.length(); ++i)
    {
        
    }
    return 0;
}